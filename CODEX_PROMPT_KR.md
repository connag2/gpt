# Codex용 최종 프롬프트 (한국어)

Python으로 로컬 영단어 암기 프로그램을 만들어줘.

## 목표
- 보기 편한 UI
- 단어를 매우 쉽게 넣을 수 있는 구조
- 자동 복습이 돌아가는 구조
- 초보자도 바로 사용할 수 있는 직관적인 프로그램

## 기술 스택
- Python 3.13
- PySide6
- JSON 파일 저장
- 모듈화 구조
- 기능 과다보다 안정성과 사용성 우선

---

## 1) 단어 입력 기능 (필수 3종)

### A. 직접 입력
- 영어 단어 입력칸 1개
- 뜻 입력칸 1개
- 추가 버튼

### B. 여러 줄 일괄 입력
- 큰 텍스트 박스 제공
- 한 번에 여러 줄 붙여넣기 가능
- 기본 문법:
  - `word=meaning1|meaning2|meaning3`

예시:
```
apple=사과
run=달리다|운영하다
light=빛|가벼운|밝은
```

### C. txt 파일 불러오기
- txt 파일을 읽어 위 문법으로 자동 파싱
- 즉시 저장 금지: **미리보기 후 저장**

---

## 2) 입력 파싱 규칙
- 줄 단위 처리
- 빈 줄 무시
- 앞뒤 공백 제거
- 영어 단어는 소문자 통일
- `=` 없는 줄은 오류 목록에 표시
- 뜻은 `|` 기준 분리
- 뜻 공백 제거 + 빈 뜻 제거
- 같은 단어는 뜻 병합
- 같은 뜻 중복 제거

예시 입력:
```
 apple = 사과
APPLE=과실
apple=사과
```

저장 결과:
- word: `apple`
- meanings: `["사과", "과실"]`

---

## 3) 저장 구조 (JSON)
아래 구조를 기본으로 해줘:

```json
{
  "words": [
    {
      "word": "apple",
      "meanings": ["사과"],
      "level": 0,
      "correct": 0,
      "wrong": 0,
      "last_review": null,
      "next_review": null,
      "favorite": false,
      "created_at": "2026-03-09T10:00:00",
      "updated_at": "2026-03-09T10:00:00"
    }
  ],
  "meta": {
    "version": 1
  }
}
```

---

## 4) 단어 추가 화면 요구사항
- 탭(또는 세그먼트)으로 입력 방식 전환
  - 직접 추가 / 여러 줄 추가 / 파일 불러오기
- 여러 줄 입력 화면 포함 요소
  - 입력창
  - 미리보기 버튼
  - 파싱 결과(추가/병합 예정) 표시
  - 오류 줄 목록 표시
  - 저장 버튼
- 저장 전 미리보기 테이블 제공

---

## 5) 단어장 보기 기능
- 등록 단어 목록
- 검색
- 단어 수정/삭제
- 뜻 추가/삭제
- 정렬 기준
  - 알파벳순
  - 최근 추가순
  - wrong 많은 순

---

## 6) 학습 기능
최소 2가지 모드 구현:
1. 영어 → 뜻 맞추기
2. 객관식 퀴즈(4지선다)

학습 로직:
- 랜덤 출제
- 복습 필요 단어 우선 출제 옵션
- 틀린 단어 자동 오답 목록 저장
- 학습 종료 후 오답만 다시 풀기

---

## 7) 숙련도/복습 규칙
단어별 필드:
- level, correct, wrong, last_review, next_review

기본 규칙:
- 정답: correct+1, level 증가(상한 5)
- 오답: wrong+1, level 일부 감소(하한 0)
- 정답 시 next_review 갱신
- 오답 시 next_review를 오늘(또는 즉시)로 설정

복습 간격 예:
- level 0 → 1일
- level 1 → 3일
- level 2 → 7일
- level 3 → 14일
- level 4+ → 30일

---

## 8) 메인 화면
한눈에 표시:
- 전체 단어 수
- 오늘 복습 필요 단어 수
- 오늘 학습한 단어 수
- 최근 틀린 단어

큰 버튼:
- 학습 시작
- 복습 시작
- 단어 추가
- 단어장 보기

---

## 9) UI 스타일 가이드
- 개발자용 툴처럼 복잡하게 만들지 말 것
- 버튼 크기/여백/구역 구분 명확히
- 폰트 가독성 우선
- 한 화면에 기능 과밀 배치 금지
- 초보자도 5초 안에 흐름 파악 가능하게

---

## 10) 코드 구조 (파일 분리)
아래 구조를 기본으로 구현해줘:

```
main.py
models.py
storage.py
word_manager.py
study_engine.py
review_scheduler.py
stats_manager.py
ui/main_window.py
ui/add_words_view.py
ui/wordbook_view.py
ui/study_view.py
ui/result_view.py
```

---

## 11) 안정성 요구사항 (중요)
- JSON 파일 없으면 자동 생성
- JSON 깨졌으면 백업 후 기본 구조 복구
- 저장 버튼 연타해도 중복 저장되지 않도록 보호
- 입력 오류 있어도 앱이 죽지 않도록 처리
- 수정/삭제/정렬 후 UI 즉시 갱신
- 함수/클래스 import 누락 없이 바로 실행 가능

---

## 12) 출력 형식 요구
- 여러 파일을 **완전한 코드**로 출력
- 파일별 코드 블록 분리
- `main.py`를 실행 진입점으로 제공
- 실행 방법(`pip install`, `python main.py`)까지 안내

---

## 13) 입력 예시 세트
예시 A:
```
apple=사과
banana=바나나
run=달리다|운영하다
light=빛|가벼운|밝은
```

예시 B:
```
 book = 책
book=예약하다
APPLE = 사과
apple=과실
```

저장 시:
- word는 소문자
- meanings는 병합 + 중복 제거

---

## 14) 품질 우선순위
1. 안정성
2. 사용성(UI 가독성)
3. 입력 편의성
4. 학습 로직 완성도
5. 확장성

기능이 충돌하면 **항상 안정성과 사용성**을 우선해줘.
